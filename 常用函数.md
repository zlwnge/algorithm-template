1.快速幂(快速计算$a_k/MOD$)
```
  int qmi(int a, int k, int MOD) {
      int res = 1;
      while(k) {
          if (k & 1) res = (LL)res * a % MOD;
          k >>= 1;
          a = (LL)a * a % MOD;
      }

      return res;
  }
```
